# Tutorials

- **Quick start / Getting started** — Set up `python-setup-lint` in your project, run your first lint pass, and understand the output.
- **Custom checks guide** ([custom-checks.md](custom-checks.md)) — Write and register custom pylint checkers using the `checkers/` framework.
- **Overlays guide** ([overlays.md](overlays.md)) — Configure tool-specific settings via overlay files without modifying the shipped config.
- **Semantic justification guide** ([semantic-justification.md](semantic-justification.md)) — Use the cross-encoder reranker pipeline to validate suppression justifications.
