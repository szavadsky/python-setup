## Target
Fix all lint violations in tests/runner/test_autofix_conflict.py

## Violations to fix
- **PT006**: Use `@pytest.mark.parametrize` with the correct argument style (list of tuples)
- **SIM108**: Use ternary expression instead of if-else assignment
- **TC003**: Move TYPE_CHECKING-only imports inside TYPE_CHECKING block

## Change
1. Read tests/runner/test_autofix_conflict.py to understand current state
3. Fix all violations listed above
4. Run `uv run ruff check --fix tests/runner/test_autofix_conflict.py`
6. Run `uv run ruff format tests/runner/test_autofix_conflict.py`

## Acceptance
- All violations listed above are fixed in tests/runner/test_autofix_conflict.py
- Ruff check passes on the file(s)
- Do NOT run tests or full lint pipeline - just fix the file