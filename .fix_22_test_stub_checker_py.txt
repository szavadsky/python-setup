## Target
Fix all lint violations in tests/checkers/test_stub_checker.py

## Violations to fix
- **I001**: Fix import ordering with `uv run ruff check --fix --select I001 tests/checkers/test_stub_checker.py`
- **RUF100**: Remove unused `# noqa` comments
- **TC004**: Move TYPE_CHECKING-only imports inside TYPE_CHECKING block
- **arg-type**: Fix argument type annotation
- **invalid-argument-type**: Fix invalid argument type
- **no-untyped-def**: Add type annotations to function definition
- **type-arg**: Add type argument to generic type (e.g., dict -> dict[str, Any])
- **unused-ignore**: Remove unused `# type: ignore` comments

## Change
1. Read tests/checkers/test_stub_checker.py to understand current state
3. Fix all violations listed above
4. Run `uv run ruff check --fix tests/checkers/test_stub_checker.py`
6. Run `uv run ruff format tests/checkers/test_stub_checker.py`

## Acceptance
- All violations listed above are fixed in tests/checkers/test_stub_checker.py
- Ruff check passes on the file(s)
- Do NOT run tests or full lint pipeline - just fix the file