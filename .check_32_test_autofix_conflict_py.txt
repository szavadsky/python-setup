## Target
Verify that fixes to tests/runner/test_autofix_conflict.py don't cause regressions

## Change
1. Run `uv run ruff check tests/runner/test_autofix_conflict.py` to verify no ruff violations remain
2. Run `uv run lint --no-fail-fast --path tests/runner/test_autofix_conflict.py` to verify no lint regressions
3. Run `uv run pytest -x -q` to verify all tests pass

## Acceptance
- Ruff check passes on tests/runner/test_autofix_conflict.py
- Lint pipeline shows no regressions on tests/runner/test_autofix_conflict.py
- All relevant tests pass
- If any check fails, rollback with `git checkout -- {file}`