## Target
Fix all lint violations in tests/checkers/test_stub_docstring_checker.py

## Violations to fix
- **C0413**: Move imports to top of file (before any non-import code)
- **PT006**: Use `@pytest.mark.parametrize` with the correct argument style (list of tuples)
- **no-any-return**: Add explicit return type annotation
- **unused-ignore**: Remove unused `# type: ignore` comments

## Change
1. Read tests/checkers/test_stub_docstring_checker.py to understand current state
3. Fix all violations listed above
4. Run `uv run ruff check --fix tests/checkers/test_stub_docstring_checker.py`
6. Run `uv run ruff format tests/checkers/test_stub_docstring_checker.py`

## Acceptance
- All violations listed above are fixed in tests/checkers/test_stub_docstring_checker.py
- Ruff check passes on the file(s)
- Do NOT run tests or full lint pipeline - just fix the file